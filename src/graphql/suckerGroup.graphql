query SuckerGroup($id: String!) {
  suckerGroup(id: $id) {
    id
    tokenSupply
    volume
    projects {
      items {
        chainId
        projectId
        token
        currency
        decimals
        version
      }
    }
  }
}