query SuckerGroup($id: String!) {
  suckerGroup(id: $id) {
    id
    tokenSupply
    volume
    paymentsCount
    projects {
      items {
        balance
        chainId
        projectId
        token
        tokenSupply
        currency
        decimals
        version
        suckerGroupId
      }
    }
  }
}