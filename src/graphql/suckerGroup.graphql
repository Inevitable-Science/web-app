query SuckerGroup($id: String!) {
  suckerGroup(id: $id) {
    id
    tokenSupply
    volume
    projects {
      items {
        balance
        chainId
        projectId
        token
        tokenSupply
        currency
        decimals
        version
        suckerGroupId
      }
    }
  }
}