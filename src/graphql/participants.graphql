query Participants(
  $where: participantFilter
  $orderBy: String
  $orderDirection: String
  $limit: Int
  $offset: Int
) {
  participants(
    where: $where
    orderBy: $orderBy
    orderDirection: $orderDirection
    limit: $limit
    offset: $offset
  ) {
    totalCount
    items {
      chainId
      address
      volume
      lastPaidTimestamp
      balance
      erc20Balance
      creditBalance
    }
  }
}
